<!DOCTYPE html>
<html>
<head>
  <title>Tuner</title>
  <style rel="stylesheet" type="text/css" href="normalize.css"></style>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <link href='http://fonts.googleapis.com/css?family=Fugaz+One|Noto+Sans' rel='stylesheet' type='text/css'>
</head>
	<body>

	  <header>

		  <div id="title">
			  <h1>Tuna!!!</h1>
		  </div>

			<p id="user_links">
				<% if user_signed_in? %>
					Signed in as: <%= current_user.email %>
					(<%= link_to "Sign Out", destroy_user_session_path, method: :delete %>)
			    <% else %>
			      <%= link_to "Sign In", new_user_session_path %> |
			      <%= link_to "Sign Up", new_user_registration_path %>
		    <% end %>
			</p>

		  <div id="nav">
				<%= render 'shared/nav' %>
			</div>
			
		</header>

		<main>

			<p class="notice"><%= notice %></p>
	  <p class="alert"><%= alert %></p>

			<%= yield %>
		</main>
		
		<footer>
			<p>Tuna is a music playlist app.</p>
		</footer>

</body>
</html>
